import { Request, Response } from 'express';

class ClassroomController {
  static addClassroom = (req: Request, res: Response) => {
    
  };

  static deleteClassroom = (req: Request, res: Response) => {

  };
}
